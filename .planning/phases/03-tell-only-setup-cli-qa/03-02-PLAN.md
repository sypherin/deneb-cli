---
phase: 03-tell-only-setup-cli-qa
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - deneb/cli.py
  - tests/test_deneb_rule.py
  - README.md
autonomous: true
requirements: [SET-01, PLAT-01, PLAT-02, PLAT-03, QA-02, QA-03, QA-04]

must_haves:
  truths:
    - "`deneb setup <model>` runs keyless + deterministic on this box, printing the ordered platform-branched setup steps (each: command + what it does + inline warnings) and executing NOTHING (the Deneb Rule)."
    - "An unknown model name gives a helpful error naming the closest option + a `deneb recommend` pointer and exits non-zero, never crashing (PLAT-03); a degraded platform still prints best-effort generic steps with a caveat."
    - "A test formally ASSERTS the Deneb Rule: no v1 tell-only path (setup/recommend/profile) executes anything - the setup module is pure and cmd_setup only builds a read-only profile + prints (QA-03)."
    - "`deneb --help` and README reflect the v1 commands: check / profile / recommend / setup (tell-only), with the paid CTA/teaser left out of scope."
    - "Full pytest suite is green and `deneb setup <top-pick>` is verified live on this Strix box (QA-02); Zach's-box live e2e is the QA-04 proxy, pasted into the SUMMARY."
  artifacts:
    - path: "deneb/cli.py"
      provides: "cmd_setup subcommand (keyless, print-only) + dispatch + updated _HELP (SET-01 display, PLAT-01/02/03)"
      contains: "def cmd_setup"
    - path: "tests/test_deneb_rule.py"
      provides: "QA-03 formal no-execution assertion over setup/recommend/profile (source purity + behavioral spy)"
      contains: "def test_"
    - path: "README.md"
      provides: "v1 commands note (check/profile/recommend/setup, tell-only)"
      contains: "deneb setup"
  key_links:
    - from: "deneb/cli.py"
      to: "deneb.setup_advisor"
      via: "cmd_setup resolves the model + generates + prints steps"
      pattern: "setup_advisor|setup_steps|resolve_model"
    - from: "deneb/cli.py"
      to: "deneb.hardware.profile_hardware"
      via: "cmd_setup builds the live profile (read-only) to branch the steps"
      pattern: "profile_hardware"
    - from: "tests/test_deneb_rule.py"
      to: "deneb.setup_advisor + deneb.cli"
      via: "asserts no execution in the tell-only paths"
      pattern: "setup_advisor|cmd_setup"
---

<objective>
Wire the tell-only setup advisory into the CLI and close the v1 QA gate. Add `deneb setup <model>`
(keyless, deterministic, engine-free, print-only) that resolves the model, reads this box, and
prints the ordered platform-branched setup steps with inline warnings - executing NOTHING (the
Deneb Rule). Then add the FORMAL no-execution assertion (QA-03) covering all three tell-only paths
(setup/recommend/profile), update `deneb --help` + README to the v1 command set, and land the live
e2e on this Strix box (QA-02; the Zach's-box run is the QA-04 proxy).

This completes v1: after this plan, a user on any common box can go profile -> recommend -> setup,
seeing exactly what to run, risk-flagged, with Deneb running none of it.

Purpose: make the setup advisory reachable + honest at the CLI, and PROVE the Deneb Rule holds.
Output: cmd_setup in deneb/cli.py (SET-01 display, PLAT-01/02/03), tests/test_deneb_rule.py (QA-03),
README/help updates, full suite green + the live `deneb setup` demo pasted into the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# This plan's upstream: the pure setup advisory it wires + asserts (from Plan 03-01).
@.planning/phases/03-tell-only-setup-cli-qa/03-01-PLAN.md

# The CLI to extend. Mirror cmd_profile / cmd_recommend EXACTLY: keyless (no _require_auth), local,
# engine-free (import only hardware + setup_advisor; NO client/loop/tools.run/run_write/execute),
# the same _flag helper, ANSI palette, main() dispatch, and _HELP conventions. Also UPDATE the
# cmd_recommend 'next: deneb setup <pick>' pointer text - setup now EXISTS (drop 'lands in a later
# release'; keep 'deneb runs nothing').
@deneb/cli.py

# The live read-only profile source cmd_setup consumes (Phase 1). profile_hardware() uses the
# allowlisted read-only tools boundary (diagnosis) - that is ALLOWED; the Deneb Rule forbids
# executing the SETUP steps, not reading the box.
@deneb/hardware.py

# The Deneb Rule (absolute) - the constraint QA-03 formally proves.
@LEVELUP-PLAN.md
</context>

<interfaces>
<!-- Contracts from Plan 03-01 (this wave's upstream). Use directly; no exploration. -->

From deneb/setup_advisor.py (03-01):
- Step(title: str, command: str, what: str, warnings: list[str], kind: str)
- setup_steps(model, profile, quant=None) -> list[Step]   # PURE, ordered runtime->download->run, never raises
- resolve_model(name) -> Model | None                     # case/separator-insensitive; None on miss
- PLAYBOOK: dict[str, ...] keyed by "cuda"/"rocm"/"metal"/"cpu"

From deneb/cli.py (existing, mirror these):
- _flag(argv, name) -> str|None
- cmd_profile(argv) / cmd_recommend(argv): keyless, local, engine-free; ANSI palette dict `_C`;
  ui.banner()/ui.error(); main() dispatches a leading subcommand; _HELP is the usage string.

From deneb/hardware.py (Phase 1): profile_hardware() -> HardwareProfile (read-only probes; allowed).

The Deneb-Rule EXECUTOR surface that tell-only paths MUST NOT touch (from deneb/tools.py):
  tools.run_write, tools.execute, tools.execute_write, tools.write_file  (WRITE/EXEC)
  plus subprocess.run / os.system / os.popen, and the engine (client.*, loop.run).
  (tools.run is READ-ONLY allowlisted diagnosis - used by profile_hardware - and is ALLOWED.)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `deneb setup <model>` CLI wiring + help/README + live e2e (SET-01, PLAT-01/02/03, QA-02)</name>
  <files>deneb/cli.py, README.md</files>
  <action>
    Add cmd_setup(argv) to deneb/cli.py mirroring cmd_recommend's shape: KEYLESS (no _require_auth),
    local, deterministic, engine-free - import ONLY hardware + setup_advisor (NO client/loop, NO
    tools.run/run_write/execute; the Deneb Rule = print only). Parse the model name as the positional
    args after "setup" (join the non-flag argv). If no name given, print a short usage line + point to
    `deneb recommend` and return 2. Call setup_advisor.resolve_model(name); if None, ui.error with a
    helpful message that names the valid catalog picks are visible via `deneb recommend --use <case>`
    (and, if easy, the closest catalog name) and return 2 (PLAT-03: honest, no crash). Otherwise build
    the live profile with hardware.profile_hardware() (read-only, allowed), call
    setup_advisor.setup_steps(model, profile), and PRINT with ui.banner() + the same ANSI palette
    cmd_profile/cmd_recommend use: a header naming the model + detected backend + a one-line "Deneb
    shows every command and runs NOTHING (the Deneb Rule)"; then each step numbered with its title,
    the exact command (highlighted), a "what: ..." line, and each warning on its own line with a
    warning marker; then a footer reminder that Deneb executed nothing and (v1) the user runs these
    themselves. Return 0. Do NOT invoke or shell out any generated command. Wire dispatch in main():
    add a branch routing a leading "setup" arg to cmd_setup(argv[1:]) alongside profile/recommend/check.
    UPDATE _HELP: add a `deneb setup <model>   print the tell-only, platform-branched setup steps
    (runs nothing)` line. Also UPDATE cmd_recommend's existing 'next: deneb setup <pick>' pointer text
    so it no longer says setup "lands in a later release" (setup exists now) while keeping the "deneb
    runs nothing" framing. Do not touch the auth/loop/one-shot paths.

    Then update README.md: add `deneb setup <model>` to the command usage list and a short note that
    v1 has four keyless commands - check / profile / recommend / setup - and that setup is TELL-ONLY
    (shows the platform-correct steps + risks, executes nothing; execution is a later tier). Keep the
    paid CTA / teaser OUT OF SCOPE (do not add pricing/upsell copy). Reference SET-01/PLAT-01 in the
    commit message. Commit atomically.
  </action>
  <verify>
    <automated>cd /home/awpapa/deneb-cli && echo '=== live e2e: setup top pick (ROCm steps, runs nothing) ===' && python3 -m deneb setup Qwen3.6-35B-A3B 2>&1 | tail -30 && echo '=== unknown model -> helpful error + exit 2 ===' && (python3 -m deneb setup not-a-real-model; echo "exit=$?") && echo '=== recommend still names the top, setup consumes it ===' && python3 -m deneb recommend --use coding 2>&1 | grep -m1 -oE 'Qwen3.6-35B-A3B' && echo '=== cmd_setup imports no executor ===' && python3 - <<'PY'
import inspect
from deneb import cli
src = inspect.getsource(cli.cmd_setup)
bad = [t for t in ('tools.run','tools.run_write','tools.execute','tools.write_file','subprocess','os.system','os.popen','client.','loop.run') if t in src]
print('cmd_setup forbidden tokens:', bad or 'NONE (clean)')
assert not bad
PY</automated>
  </verify>
  <done>`deneb setup Qwen3.6-35B-A3B` runs live on this box keyless, printing ordered ROCm-branched steps (runtime -> download with a GB warning -> run with the 127.0.0.1:8001 + gfx1151 warnings), executing nothing; `deneb setup not-a-real-model` prints a helpful error pointing at `deneb recommend` and exits 2; `deneb --help` + README list setup; the recommend pointer text is updated; cmd_setup body contains no executor token. Full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: QA-03 Deneb-Rule no-execution assertion + full-suite green (QA-03, QA-04-proxy)</name>
  <files>tests/test_deneb_rule.py</files>
  <behavior>
    Write tests/test_deneb_rule.py asserting the Deneb Rule holds across ALL v1 tell-only paths
    (setup / recommend / profile). Mirror the repo test style (sys.path insert; runnable via
    `python3 tests/test_deneb_rule.py` and collectable by pytest). Cover both a SOURCE assertion and
    a BEHAVIORAL spy:

    - SOURCE PURITY (setup_advisor): read deneb/setup_advisor.py source (or inspect.getsource on the
      module) and assert it contains NONE of: "subprocess", "os.system", "os.popen", "Popen",
      "tools.run", "tools.execute", "tools.write", "run_write", " exec(", " eval(", and does not
      `import` the tools module. (setup_steps must be pure - it only returns Step structs.)
    - SOURCE PURITY (CLI tell-only fns): for each of cli.cmd_setup, cli.cmd_recommend, cli.cmd_profile,
      inspect.getsource(fn) and assert it contains NONE of: "tools." , "subprocess", "os.system",
      "os.popen", "client.", "loop.run", "run_write", "execute". (These fns may call profile_hardware,
      which internally uses the read-only tools.run boundary - that is diagnosis, allowed - but the fn
      bodies themselves reference no executor.)
    - BEHAVIORAL SPY (the strong proof): monkeypatch the WRITE/EXEC surface to FAIL LOUD - replace
      deneb.tools.run_write, deneb.tools.execute, deneb.tools.execute_write, deneb.tools.write_file,
      and os.system with a stub that raises AssertionError("Deneb Rule violated - a tell-only path
      executed a command"). (Leave the read-only tools.run intact so profile_hardware still reads the
      box.) Then: (a) call setup_advisor.setup_steps(resolve_model('Qwen3.6-35B-A3B'), a constructed
      rocm profile) and assert it returns a non-empty list[Step] and fired NONE of the stubs; (b) call
      cli.cmd_setup(['Qwen3.6-35B-A3B']) capturing stdout, assert it returns 0, printed non-empty
      output that includes the run command text (e.g. 'llama-server'), and fired NONE of the stubs;
      (c) call cli.cmd_recommend(['--use','coding']) and cli.cmd_profile([]) and assert neither fired
      a stub. (Use capsys/redirect for stdout; restore the patched attrs in teardown/finally.)
    - PRINTS-ONLY: assert cmd_setup returns an int and the setup step commands appear in stdout as
      TEXT (advice shown, not run) - the run-step warning about 127.0.0.1:8001 is present.

    The whole file must itself execute nothing destructive - the fail-loud stubs prove absence of
    execution; nothing here runs a real setup command.
  </behavior>
  <action>
    Implement tests/test_deneb_rule.py per the behavior block: a source-purity check over
    deneb/setup_advisor.py + the three CLI tell-only functions, and a behavioral spy that fail-loud-
    patches the tools write/exec surface + os.system and drives setup_steps + cmd_setup + cmd_recommend
    + cmd_profile, asserting none fire. Construct profiles directly (reuse the strix_box helper shape
    from tests/test_recommend.py). Keep patches scoped + restored (monkeypatch fixture or try/finally).
    This is the formal QA-03 gate proving the Deneb Rule. Then run the FULL suite and ensure it is
    green. Commit atomically (test(03-02): QA-03 Deneb-Rule no-execution assertion). In the SUMMARY,
    paste the live `deneb setup Qwen3.6-35B-A3B` output from this box (the QA-02 e2e + QA-04 proxy) and
    the final `pytest -q` green count.
  </action>
  <verify>
    <automated>cd /home/awpapa/deneb-cli && python3 -m pytest tests/test_deneb_rule.py -q 2>&1 | tail -20 && echo '=== FULL SUITE (v1 final gate) ===' && python3 -m pytest -q 2>&1 | tail -4</automated>
  </verify>
  <done>tests/test_deneb_rule.py passes: source purity of setup_advisor + cmd_setup/cmd_recommend/cmd_profile, and the behavioral spy proves setup_steps/cmd_setup/cmd_recommend/cmd_profile execute NOTHING (fail-loud stubs never fire) while still printing the advice. Full suite green (83 prior + Plan 03-01's + these). Live `deneb setup` demo + final green count captured in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `setup <model>` arg -> cmd_setup | user-supplied model name; unknown/garbage must yield a helpful error + exit 2, never crash |
| HardwareProfile -> cmd_setup/setup_steps | a degraded live profile must yield generic steps + a caveat, never an exception |
| generated setup commands -> stdout | shown as TEXT only; NO tell-only path (setup/recommend/profile) executes them (the Deneb Rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Elevation of Privilege | a v1 tell-only path executes a setup command (Deneb Rule violation) | mitigate | cmd_setup is print-only, keyless, engine-free (imports only hardware + setup_advisor); QA-03 asserts source purity of setup_advisor + the three CLI fns AND a behavioral fail-loud spy proving setup_steps/cmd_setup/cmd_recommend/cmd_profile fire no write/exec primitive |
| T-03-22 | Tampering | unknown/garbage `setup <model>` arg | mitigate | resolve_model returns None -> cmd_setup prints a helpful `deneb recommend` pointer + exits 2; never crashes (PLAT-03) |
| T-03-23 | Information Disclosure | the advised run command could expose the model on 0.0.0.0 | mitigate | the run step binds 127.0.0.1 only (from 03-01); the CLI prints it verbatim, adds no exposure step (exposure is the paid tier, out of v1 scope) |
| T-03-24 | Denial of Service | cmd_setup on a degraded/None-budget profile | mitigate | setup_steps is guarded (03-01) -> generic fallback steps; cmd_setup handles the None/degraded profile without raising |
| T-03-SC | Tampering | supply chain (pip installs) | accept | no new packages - stdlib + already-present pytest only; nothing to audit |
</threat_model>

<verification>
- `python3 -m pytest tests/test_deneb_rule.py -q` green: source purity (setup_advisor + cmd_setup/recommend/profile) + the behavioral no-execution spy.
- LIVE e2e (QA-02, this Strix box): `python3 -m deneb setup Qwen3.6-35B-A3B` prints ordered ROCm-branched steps (runtime -> download with a ~GB warning -> run with 127.0.0.1:8001 + gfx1151 warnings) and executes nothing; `python3 -m deneb setup not-a-real-model` prints a helpful error + exits 2.
- `deneb --help` and README list all four v1 commands (check/profile/recommend/setup); no paid CTA added.
- FINAL v1 GATE: `python3 -m pytest -q` fully green (Phase 1+2's 83 + Plan 03-01's + these).
- QA-04 proxy: the live `deneb setup` run on this box (Zach's Strix Halo) pasted into the SUMMARY.
</verification>

<success_criteria>
- `deneb setup <model>` keyless/deterministic/engine-free, prints the ordered platform-branched steps with inline warnings, handles unknown-model + degraded-platform honestly (SET-01 display, PLAT-01/02/03), verified live on this box (QA-02).
- The Deneb Rule is formally proven: no tell-only path (setup/recommend/profile) executes anything - source purity + behavioral spy (QA-03).
- `deneb --help` + README reflect the v1 command set (check/profile/recommend/setup, tell-only); paid CTA/teaser deferred.
- Full test suite green; v1 complete.
</success_criteria>

<output>
Create `.planning/phases/03-tell-only-setup-cli-qa/03-02-SUMMARY.md` when done. This completes v1:
end the SUMMARY with the full-suite green count + the live `deneb setup Qwen3.6-35B-A3B` demo output
pasted from this box.
</output>
